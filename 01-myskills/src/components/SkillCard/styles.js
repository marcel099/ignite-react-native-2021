import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  skillButton: {
    backgroundColor: '#1f1e25',
    padding: 15,
    borderRadius: 50,
  },
  skill: {
    color: '#fff',
    fontSize: 22,
    fontWeight: 'bold',
  },
});
