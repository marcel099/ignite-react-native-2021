import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  skillButton: {
    backgroundColor: '#1f1e25',
    padding: 15,
    borderRadius: 50,
    marginBottom: 16,
  },
  skill: {
    color: '#fff',
    fontSize: 22,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
